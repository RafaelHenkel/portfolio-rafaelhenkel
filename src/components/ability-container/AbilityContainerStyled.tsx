import styled from 'styled-components';

const AbilityContainerStyled = styled.section`
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(18rem, 1fr));
  grid-gap: 20px;
  width: 100%;
  min-height: 100%;
`;

export default AbilityContainerStyled;
